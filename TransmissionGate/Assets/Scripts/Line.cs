using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Line : MonoBehaviour
{
    public Transform other;
    public Color color;
    // Update is called once per frame
    void Update()
    {
      // if (other)
      //     Debug.DrawLine(transform.position, other.position, color);
    }
}
